import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mstraybiz.multilingualbaptisthymnal',
  appName: 'Baptist Hymnal',
  webDir: 'build'
};

export default config;
